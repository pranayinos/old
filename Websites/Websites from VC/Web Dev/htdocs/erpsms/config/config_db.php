<?php
define( DBHOST, "localhost",     true ); // The true makes it so you have to type in DBHOST
define( DBUSER, "root",        true );    // in caps to retreive the<span id="IL_AD5" class="IL_AD">string</span>.
define( DBPASS, "",        true );
define( DBNAME, "e_school_db", true );
 /*
define( DBHOST, "www.dnbmis.com",     true ); // The true makes it so you have to type in DBHOST
define( DBUSER, "bsnl_dhanbad_326",        true );    // in caps to retreive the <span id="IL_AD5" class="IL_AD">string</span>.
define( DBPASS, "dhanbad@326",        true );
define( DBNAME, "bsnl_db", true );
*/
?>